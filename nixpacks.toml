[phases.setup]
providers = ["nodejs_18", "npm-9_x", "openssl", "curl", "wget"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"